# Design System

## Style Prompt

以 xAI 式研究实验室界面的克制为起点，服务于圣农企业架构评审，但不复刻其营销页面。整站采用“经营案卷”语言：近黑纸面、中文主叙事、少量等宽编号和一条橙色责任线。页面应像经过编辑的管理材料，而不是组件库展示、彩色仪表盘或 AI 自动生成的演示稿。保留九节点导航、业务证据、状态、表格和查询循环，删去背景巨字、重复英文眉题、等权卡片墙和装饰性动效。

## Colors

- Canvas: `#0a0a0a`
- Canvas Soft: `#1a1c20`
- Canvas Card: `#191919`
- Canvas Mid: `#363a3f`
- Hairline: `#212327`
- Ink: `#ffffff`
- Body: `#dadbdf`
- Muted: `#7d8187`
- Active / Sunset: `#ff7a17`
- Active Soft: `#ffc285`
- Dusk: `#7c3aed`
- Twilight: `#c4b5fd`
- Breeze: `#a0c3ec`

白色负责主要层级。橙色只用于当前节点、关键责任门和少量操作反馈。蓝色与紫色只用于区分必要的能力状态，不形成彩色卡片墙。

## Typography

- Display and body: PingFang SC 400，回退为 Noto Sans SC、Microsoft YaHei。
- Technical labels and code: SFMono-Regular 400，回退为 Geist Mono、JetBrains Mono、Consolas。
- 所有角色默认 400 字重，依靠字号、留白和明度形成层级。
- 技术眉题使用大写或代码式标签，字号 12-14px。
- 出于当前产品规范，字距保持 0，不使用负字距。

## Scale

- Hero: 72/78 desktop，48/52 tablet，40/44 mobile。
- Section title: 56/64 desktop，40/46 tablet，36/42 mobile。
- Cluster title: 32/36。
- Body large: 18/28。
- Body: 16/24。
- Secondary: 14/20。
- Mono caption: 12/16。

每个屏幕最多使用 4-6 个字号。中文业务标题是第一视觉，英文只保留在真实技术字段、接口和代码中。

## Layout

- 页面使用单一近黑画布，内容最大宽度约 1200-1280px。
- 桌面章节上下内边距 64-96px，移动端 48-64px。
- 顶部保留九节点粘性进度导航，当前节点由文字、序号、颜色和 `aria-current` 同时表达。
- 每个节点正文完整展开，不依赖隐藏标签页或右侧详情。
- 高密度表格允许横向滚动，页面本身不得横向溢出。
- 每屏只保留一个主信息；说明文字与主标题采用非对称双栏，业务图示在下方承接。
- 留白用于区分因果和阶段。节点、表格和流程可使用边框，普通说明不得默认装进卡片。

## Components

- Interactive: 1px 半透明白色描边胶囊，透明背景，最小触控高度 44px。
- Card: 只用于确有独立边界的事件、任务和数据对象；`#191919` 实体背景、`#212327` 细边框、4-8px 圆角、无阴影。
- Panel: 与页面同色或轻微深灰，不使用玻璃模糊与投影。
- Table: 表头使用 Geist Mono，行分隔使用 hairline。
- Status: 小型胶囊；文字必须说明状态，颜色不能成为唯一依据。
- Narrative ledger: 普通说明和多项事实优先使用上下分隔线与留白，不使用重复卡片。

## Motion

- 动效必须属于反馈、定位、强调或短暂愉悦之一；无法归类的动画删除。
- 按钮与悬停反馈 100-180ms，节点定位 200-360ms，章节强调 360-560ms；统一使用 `cubic-bezier(0.16, 1, 0.3, 1)`。
- 只动画 opacity 与 transform，不通过动效改变布局尺寸，不延迟内容访问。
- 进入节点时依次呈现编号、标题、说明、本节点产物和正文承接，帮助读者理解当前所在位置。
- 滚动节点变化用于表达当前阶段，不进行滚动劫持或视差。Aily 查询循环可以持续播放，因为它直接解释“由点及线、由线及面”的工作机制；其他无限装饰动画禁止。
- `prefers-reduced-motion` 下关闭平滑滚动和章节进入动画，并把循环过程展开为静态完整状态。

## What Not To Do

- 不使用阴影、玻璃拟态、渐变文字、霓虹光晕或装饰性背景网格。
- 不把页面变成营销落地页，不隐藏架构细节。
- 不用粗体堆叠层级，不使用大量填充按钮。
- 不使用纯装饰色块，不让多种强调色争夺注意力。
- 不改变现有业务事实、能力边界和验收结论。
- 不用英文标签重复已经清楚的中文标题，不在章节背景放置巨大英文或数字水印。
- 不用相同尺寸的卡片网格承载所有内容；只有真正独立、可比较的业务对象才使用卡片。
